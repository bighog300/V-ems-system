import { sqlValue } from "../db.mjs";
function map(row){return row?{...row}:undefined;}
export class StockUsageRepository { constructor(db){this.db=db;} find(id){return map(this.db.queryOne(`SELECT * FROM stock_usage WHERE stock_usage_id=${sqlValue(id)};`));} create(u){this.db.execute(`INSERT INTO stock_usage (patient_case_id,stock_usage_id,intervention_id,incident_id,encounter_id,stock_item_id,vehicle_id,quantity_used,usage_source,performed_at,intervention_type,correlation_id,discrepancy_status,created_at) VALUES (${sqlValue(u.patient_case_id)},${sqlValue(u.stock_usage_id)},${sqlValue(u.intervention_id)},${sqlValue(u.incident_id)},${sqlValue(u.encounter_id)},${sqlValue(u.stock_item_id)},${sqlValue(u.vehicle_id)},${sqlValue(u.quantity_used)},${sqlValue(u.usage_source)},${sqlValue(u.performed_at)},${sqlValue(u.intervention_type)},${sqlValue(u.correlation_id)},${sqlValue(u.discrepancy_status)},${sqlValue(u.created_at)});`);} }
