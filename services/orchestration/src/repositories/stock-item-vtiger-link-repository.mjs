import { sqlValue } from "../db.mjs";
function map(row) { return row ? { ...row } : undefined; }
export class StockItemVtigerLinkRepository {
  constructor(db) { this.db = db; }
  findByStockItemId(id) { return map(this.db.queryOne(`SELECT * FROM stock_item_vtiger_links WHERE stock_item_id=${sqlValue(id)};`)); }
  upsert(link) { const e=this.findByStockItemId(link.stock_item_id); if(!e)this.db.execute(`INSERT INTO stock_item_vtiger_links (stock_item_id,remote_id,remote_number,external_key,create_correlation_id,last_correlation_id,sync_status,last_error_code,last_synced_at,created_at,updated_at) VALUES (${sqlValue(link.stock_item_id)},${sqlValue(link.remote_id)},${sqlValue(link.remote_number)},${sqlValue(link.external_key)},${sqlValue(link.create_correlation_id)},${sqlValue(link.last_correlation_id)},${sqlValue(link.sync_status??"pending")},${sqlValue(link.last_error_code)},${sqlValue(link.last_synced_at)},${sqlValue(link.created_at)},${sqlValue(link.updated_at)});`); else this.db.execute(`UPDATE stock_item_vtiger_links SET remote_id=${sqlValue(link.remote_id??e.remote_id)},remote_number=${sqlValue(link.remote_number??e.remote_number)},external_key=${sqlValue(link.external_key??e.external_key)},last_correlation_id=${sqlValue(link.last_correlation_id??e.last_correlation_id)},sync_status=${sqlValue(link.sync_status??e.sync_status)},last_error_code=${sqlValue(link.last_error_code)},last_synced_at=${sqlValue(link.last_synced_at)},updated_at=${sqlValue(link.updated_at??new Date().toISOString())} WHERE stock_item_id=${sqlValue(link.stock_item_id)};`); }
  markFailure(id,code,status,at){this.db.execute(`UPDATE stock_item_vtiger_links SET sync_status=${sqlValue(status)},last_error_code=${sqlValue(code)},updated_at=${sqlValue(at)} WHERE stock_item_id=${sqlValue(id)};`);}
}
