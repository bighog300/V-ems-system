import { sqlValue } from "../db.mjs";
function map(row){return row?{...row}:undefined;}
export class VehicleStockVtigerLinkRepository { constructor(db){this.db=db;} find(vehicleId,itemId){return map(this.db.queryOne(`SELECT * FROM vehicle_stock_vtiger_links WHERE vehicle_id=${sqlValue(vehicleId)} AND stock_item_id=${sqlValue(itemId)};`));} upsert(l){const e=this.find(l.vehicle_id,l.stock_item_id);if(!e)this.db.execute(`INSERT INTO vehicle_stock_vtiger_links (vehicle_id,stock_item_id,remote_id,remote_number,external_key,create_correlation_id,last_correlation_id,sync_status,last_error_code,last_synced_at,created_at,updated_at) VALUES (${sqlValue(l.vehicle_id)},${sqlValue(l.stock_item_id)},${sqlValue(l.remote_id)},${sqlValue(l.remote_number)},${sqlValue(l.external_key)},${sqlValue(l.create_correlation_id)},${sqlValue(l.last_correlation_id)},${sqlValue(l.sync_status??"pending")},${sqlValue(l.last_error_code)},${sqlValue(l.last_synced_at)},${sqlValue(l.created_at)},${sqlValue(l.updated_at)});`);else this.db.execute(`UPDATE vehicle_stock_vtiger_links SET remote_id=${sqlValue(l.remote_id??e.remote_id)},remote_number=${sqlValue(l.remote_number??e.remote_number)},external_key=${sqlValue(l.external_key??e.external_key)},last_correlation_id=${sqlValue(l.last_correlation_id??e.last_correlation_id)},sync_status=${sqlValue(l.sync_status??e.sync_status)},last_error_code=${sqlValue(l.last_error_code)},last_synced_at=${sqlValue(l.last_synced_at)},updated_at=${sqlValue(l.updated_at??new Date().toISOString())} WHERE vehicle_id=${sqlValue(l.vehicle_id)} AND stock_item_id=${sqlValue(l.stock_item_id)};`);} markFailure(v,i,c,s,a){this.db.execute(`UPDATE vehicle_stock_vtiger_links SET sync_status=${sqlValue(s)},last_error_code=${sqlValue(c)},updated_at=${sqlValue(a)} WHERE vehicle_id=${sqlValue(v)} AND stock_item_id=${sqlValue(i)};`);}}
