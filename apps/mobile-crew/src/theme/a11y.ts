// Ambulance crews often work these screens with gloved hands and in motion —
// touch targets are sized well above the 44x44 WCAG minimum for primary
// actions. CONTENT_MAX_WIDTH keeps forms from stretching edge-to-edge into
// unreadable full-bleed rows on a tablet.
export const TOUCH_TARGET_MIN = 56;
export const CHIP_TARGET_MIN = 44;
export const CONTENT_MAX_WIDTH = 720;
